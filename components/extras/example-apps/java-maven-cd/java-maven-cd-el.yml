apiVersion: triggers.tekton.dev/v1beta1
kind: EventListener
metadata:
  name: java-maven-cd
spec:
  namespaceSelector: {}
  resources: {}
  serviceAccountName: pipeline
  triggers:
  - bindings:
    - kind: ClusterTriggerBinding
      ref: github-push
    template:
      ref: java-maven
