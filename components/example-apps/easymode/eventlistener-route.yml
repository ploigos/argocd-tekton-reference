apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: easymode
spec:
  port:
    targetPort: 8080
  tls:
    insecureEdgeTerminationPolicy: Redirect
    termination: edge
  to:
    kind: Service
    name: easymode
    weight: 100
  wildcardPolicy: None
